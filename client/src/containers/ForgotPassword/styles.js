import { styled } from '@mui/system';

export const ForgotPasswordSection = styled('section')(({ theme }) => ({
  background: theme.palette.secondary.main,
  padding: '12.5rem 0',
  minHeight: 'calc(100vh - 20rem)',
}));
