import { styled } from '@mui/system';

export const Image = styled('img')({
  height: 200,
  width: 200,
  borderRadius: '50%',
  overflow: 'hidden',
  display: 'block',
  margin: '0 auto',
  outlineOffset: 'inherit',
});
